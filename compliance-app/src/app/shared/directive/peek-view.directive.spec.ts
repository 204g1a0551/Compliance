import { PeekViewDirective } from './peek-view.directive';

describe('PeekViewDirective', () => {
  it('should create an instance', () => {
    const directive = new PeekViewDirective();
    expect(directive).toBeTruthy();
  });
});
