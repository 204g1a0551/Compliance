import { Routes } from '@angular/router';
import { Dashboard } from './shared/module/dashboard/dashboard';

export const routes: Routes = [
    {
        path: '',
        component: Dashboard
    }
];
